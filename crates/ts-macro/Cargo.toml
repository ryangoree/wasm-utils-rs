[package]
name = "ts-macro"
version = "0.0.0"
edition = "2021"

[lib]
proc-macro = true

[dependencies]
# external
heck = "0.5.0"
quote = "1.0"
syn = { version = "1.0", features = "full" }
wasm-bindgen = "0.2.92"

# workspace
ts-type = { path = "../ts-type" }